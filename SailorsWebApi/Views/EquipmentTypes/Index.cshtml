@model IEnumerable<SailorsWebApi.Models.EquipmentTypes>

@{
    ViewBag.Title = "Index";
}

<h2>Typy sprzętu</h2>

<p>
    @Html.ActionLink("Utwórz nowy", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.typeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.typeName)
        </td>
        <td>
            @Html.ActionLink("Edytuj", "Edit", new { id=item.typeId }) |
            @Html.ActionLink("Usuń", "Delete", new { id=item.typeId })
        </td>
    </tr>
}

</table>
