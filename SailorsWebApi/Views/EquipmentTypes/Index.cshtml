@model IEnumerable<SailorsWebApi.Models.EquipmentTypes>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.typeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.maxUsers)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.typeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.maxUsers)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.typeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.typeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.typeId })
        </td>
    </tr>
}

</table>
