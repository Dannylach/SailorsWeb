@model IEnumerable<SailorsWebApi.Models.ScoutRanks>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.scoutRankName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.scoutRankName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.scoutRankId }) |
            @Html.ActionLink("Details", "Details", new { id=item.scoutRankId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.scoutRankId })
        </td>
    </tr>
}

</table>
