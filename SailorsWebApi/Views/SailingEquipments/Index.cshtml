@model IEnumerable<SailorsWebApi.Models.SailingEquipment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.equipmentName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EquipmentTypes.typeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.equipmentName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EquipmentTypes.typeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.equipmentId }) |
            @Html.ActionLink("Details", "Details", new { id=item.equipmentId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.equipmentId })
        </td>
    </tr>
}

</table>
